head
    title='Mclaren Library'
    link(rel='stylesheet' href='styles.css')
body
    h1=message
    p #{list[0]._id}
    ul
    each item in list
        li #{item.title}

a(href='/form') AddNewBook